apiVersion: v1
kind: Service
metadata:
    name: "portfoliokubernetescluster-7642"
    labels:
        app: "portfoliokubernetescluster-7642"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "portfoliokubernetescluster-7642"